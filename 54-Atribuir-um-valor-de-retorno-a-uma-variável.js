// Atribuir um valor de retorno a uma variável em JavaScript.

function somar(a,b) {
    return a + b;
}

// O valor retornado é atribuído a uma variável 

var resultado = somar(5,3);
console.log(resultado);

// exemplo

function criarUmaMeta(linguagemDeProgramação) {
    return " minha meta é aprender " + linguagemDeProgramação;
}

var minhaMeta = criarUmaMeta("JavaScript");
console.log(minhaMeta);
    
